class Solution {
public:
    
    bool stoneGame(vector<int>& piles) {
        // int n = piles.size();
        // dp.assign(n, vector<int>(n, INT_MIN));

        // return dfs(0, n - 1, piles) > 0;
        return true;
    }
};